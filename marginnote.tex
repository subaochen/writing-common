% 在页边显示一个代码索引序号
% 只能用在一个包含代码的listings环境中，不能使用在
% inputlstlisting环境中
% 使用方法：
% /* (*@\srcidx{\vref{...}}@*) */
\newcommand{\srcidx}[1]{%
    \makebox[0pt][l]{%
        \makebox[\linewidth+\marginparsep][l]{}%
            \fbox{#1}%
    }%
}
